
'use server';

/**
 * @fileOverview This flow is now deprecated.
 * The campaign generation process has been broken down into smaller, granular flows
 * orchestrated by the client in `src/app/play/page.tsx` to allow for progress updates.
 * This unified server-side flow is no longer used.
 */
